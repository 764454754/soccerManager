import React, { Fragment } from 'react';

export default class Some extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
        };
    }

    render() {
        return <Fragment>
            test router
        </Fragment>;
    }
}
